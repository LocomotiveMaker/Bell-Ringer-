# FinalDemo Rain/Forest Visual Fix - 2026-05-31

Context:
- User reported the rain looked sideways, ground drops showed black cutout boxes, and the forest looked broken/oversized.
- User also asked whether `DistantFogBand` and `DarkSkyPlane` can be removed, and whether enlarging `Ground_Grey_Authoring` matters.
- Continue preserving authored bell follow positions, tinnitus positions, boss positions, and boss weakpoint path positions.

Important clarification:
- The Asset Store packages were not actually imported into this workspace. The previous implementation used local/procedural fallback visuals.
- The new pass fixes those fallbacks so the demo can run without package import.
- When the packages are imported later through Unity Package Manager, the procedural forest/rain can be replaced intentionally.

Completed:
- Reworked `FinalDemoWorldRainEnvironment`:
  - Rain now uses a procedural transparent streak texture.
  - Rain particles start at zero speed and fall vertically with velocity-over-lifetime.
  - Wind drift is tiny by default, so rain should read as falling down instead of flowing sideways.
  - Ground ripples now use a procedural transparent ripple texture, removing black square cutouts.
- Reworked `FinalDemoForestEnvironment`:
  - Default forest now uses stable procedural trees and ground clutter, not imported FBX resources.
  - This avoids bad package scale/materials until the real Asset Store package is imported.
  - Forest motes now receive an explicit particle material, avoiding magenta/missing-material particles.
- `DistantFogBand` and `DarkSkyPlane` are now included in runtime/editor cleanup lists and are no longer generated by the scene builder.
- `Ground_Grey_Authoring` remains safe to enlarge in the scene, but runtime cleanup replaces it with `FinalDemoWorldFloorSurface`.
- Added low-priority pad LED anchor feedback through `FinalDemoLightRouter.ShowPadAnchor`.
- Added `FinalDemoGlitchVisual` to tinnitus and boss tinnitus visuals for lightweight object-space glitch feedback.
- `FinalDemoPolishTool` applies the runtime rain/forest/floor objects, glitch visuals, model presenter, and cleanup when run from Unity.

Validation:
- `powershell -ExecutionPolicy Bypass -File tools\Build-DotNetProject.ps1 -ProjectPath BellRinger.Runtime.csproj` passes.
- `powershell -ExecutionPolicy Bypass -File tools\Build-DotNetProject.ps1 -ProjectPath BellRinger.Editor.csproj` passes.
- Existing warnings remain unrelated: `PadTrackingReceiver.PadTrackingPacket` DTO fields and `USG0001`.

QA focus:
- In `FinalDemo`, reach the rain stage without `Force Next`; rain audio and vertical world rain should begin during `BellFollowRain`.
- Check that ground ripples have no black square backgrounds.
- Check that forest stage no longer creates huge broken imported trees or magenta particles.
- Check that the 16x8 preview still responds to bell/tinnitus/rain, and the physical board output remains on COM40.
- If Unity still shows old scene objects, run `Bell Ringer/Final Demo/Apply Polish` after closing other Unity instances, or rely on runtime cleanup during Play.

Notebook compatibility:
- No COM port, hardware sketch, serial default, or platform-specific path changed.
- Runtime fallback visuals use standard Unity primitives/particles and should remain notebook-compatible.
